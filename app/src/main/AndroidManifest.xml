<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed only for local Wi-Fi/hotspot device-to-device sync (Sync tab).
         Android requires INTERNET for any socket use, but this app never talks
         to the public internet - only to the other authorized phones on the
         same Wi-Fi/hotspot, and only while a sync is actively running. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Needed for the "Take Photo" option when adding/editing a member. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- SecuGen USB fingerprint scanner (enroll + check-in verification).
         android.permission.USB_PERMISSION is the SDK's own action name, not a
         real platform permission, but it's declared/matched here as the SDK
         expects. required="false" so the app still installs on tablets/phones
         without a USB-OTG host port; the scanner features are simply hidden
         if no compatible device is ever attached. -->
    <uses-permission android:name="android.permission.USB_PERMISSION" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <!-- Kiosk mode: lets the fingerprint scanner keep listening while the app is
         backgrounded (Home button), per the agreed spec. connectedDevice is the
         foreground-service type Android 14+ requires be declared for USB-device
         work. POST_NOTIFICATIONS is needed on Android 13+ for the (mandatory,
         OS-enforced) ongoing notification to actually show. USE_FULL_SCREEN_INTENT
         lets a recognized/not-recognized result bring the app back to the front
         automatically instead of just sitting as a tappable notification. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.NoActionBar"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- So the OS routes "USB scanner plugged in" straight to this activity
                 (e.g. front-desk tablet where the reader is attached mid-session). -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>

        <!-- Kiosk mode background listener — see FingerprintKioskService for full
             lifecycle notes (starts only once a scanner is detected, survives Home,
             stops if the app is swiped from Recent Apps). -->
        <service
            android:name=".kiosk.FingerprintKioskService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Lets QrShareUtils hand a QR bitmap to other apps (WhatsApp, Gallery,
             Print, etc.) via a content:// URI instead of a raw file:// path,
             which newer Android versions block. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
